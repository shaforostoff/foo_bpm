# CMake build trees, the fetched SDK and WTL, and the packaged component.
/build
/external
/dist

# Visual Studio / CMake user state
/.vs
/CMakeUserPresets.json
*.user

# The analysis pipeline's scratch lives under /build, which is already ignored.
__pycache__/
*.pyc
